[pytest]
addopts = -ra
markers =
    catalogue(id, steps): maps a runtime test to the frozen catalogue test ID and step coverage
    known_gap: a catalogue substep that cannot yet execute because the public capability is absent
